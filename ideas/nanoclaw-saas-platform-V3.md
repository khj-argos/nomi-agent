# NanoClaw SaaS 플랫폼 — "나만의 AI를 키우는 플랫폼"

> 저장일: 2026-03-08 / 업데이트: 2026-03-08
> 상태: 아이디어 단계 (구현 미착수)  
> 연관 파일: 없음

---

## 핵심 컨셉

NanoClaw를 사용자별로 완전히 독립된 인스턴스로 제공하는 클라우드 플랫폼.
공유 인프라 없음 — 사용자마다 자신만의 NanoClaw가 생성되고, 사용할수록 나를 닮아가는 AI로 진화.

**한 줄 정의**: "쓸수록 나를 닮아가는 나만의 AI — 부르지 않아도 알아서 찾아오는 AI"

> 내부 개발 컨셉: 다마고치 — 성장하고 진화하는 나만의 에이전트 (브랜딩 아님)

---

## 핵심 원칙

| 원칙                | 내용                                                                  |
| ------------------- | --------------------------------------------------------------------- |
| **인프라만 제공**   | 우리는 NanoClaw가 돌아갈 서버/컨테이너 환경만 제공                    |
| **LLM은 사용자 것** | 기본: 사용자가 본인 Anthropic API 키 직접 입력                        |
| **LLM 옵션 제공**   | 옵션: 우리가 제공하는 LLM 사용 (추가 요금 or 번들)                    |
| **완전 격리**       | 사용자 간 데이터/컨테이너/메모리 완전 분리                            |
| **자동 진화**       | 대화 패턴, 습관, 선호도를 에이전트가 스스로 파악해 CLAUDE.md 업데이트 |
| **능동적 행동**     | 에이전트가 스스로 판단해서 먼저 말 걸어옴 — 사용자가 열지 않아도 동작 |

---

## 사용자 경험 플로우

```
1. 가입
      ↓
2. LLM 설정
   - (기본) 본인 Anthropic API 키 입력
   - (옵션) 우리가 제공하는 LLM 플랜 선택
      ↓
3. 온보딩 — 나만의 AI 설정 (Web UI)
   - 계층 1: 원클릭 템플릿 선택
   - 계층 2: 자연어로 원하는 AI 묘사
   - 계층 3: 직접 편집 (고급)
      ↓
4. 나만의 NanoClaw 인스턴스 생성
   - 독립 컨테이너
   - 독립 SQLite DB
   - 독립 CLAUDE.md (온보딩 설정 반영)
      ↓
5. 채널 연결 (WhatsApp / Telegram / Slack 등)
      ↓
6. 에이전트가 능동적으로 동작 시작
   - 정해진 시간에 먼저 말 걸어옴
   - 상황 감지해서 알아서 찾아옴
   - 쓸수록 나를 닮아가며 진화
      ↓
7. 시간이 지날수록 "내 AI"가 됨
```

```
1. 가입
      ↓
2. Anthropic API 키 입력
   (또는 우리 LLM 플랜 선택)
      ↓
3. 나만의 NanoClaw 인스턴스 생성
   - 독립 컨테이너
   - 독립 SQLite DB
   - 독립 CLAUDE.md (나만의 메모리)
      ↓
4. 채널 연결 (WhatsApp / Telegram / Slack 등)
      ↓
5. 사용하면서 자동 학습/진화
   - 대화 패턴 파악
   - 사용자 습관/선호도 축적
   - 에이전트 성격이 사용자를 닮아감
      ↓
6. 시간이 지날수록 "내 AI"가 됨
```

---

## LLM 전략

### Phase 1 — Claude 전용 (시작)

| 옵션 | 설명 | 사용자 비용 |
| ---- | ---- | --------- |
| **BYOK — Claude (기본)** | 사용자가 본인 Anthropic API 키 직접 입력 | API 사용량 직접 부담 + 플랫폼 구독료 |
| **우리 LLM — Ollama (옵션)** | 우리 서버에서 오픈소스 LLM 제공 (Llama, Mistral 등) | 플랫폼 구독료에 포함 or 추가 요금 |

**BYOK가 기본인 이유:**
- 우리는 인프라 비즈니스 — LLM 마진 리스크 없음
- 사용자가 비용 컨트롤 가능
- Anthropic 약관 준수 용이
- 사용자 신뢰도 높음 (내 키 = 내 데이터)

### Phase 2 — 멀티 LLM 지원 (확장)

| LLM | 방식 | 비고 |
| --- | ---- | ---- |
| Claude (Anthropic) | BYOK + 우리 제공 | Phase 1부터 지원 |
| GPT-4o (OpenAI) | BYOK | 사용자가 OpenAI API 키 입력 |
| Gemini (Google) | BYOK | 사용자가 Google API 키 입력 |
| 오픈소스 LLM | 우리 Ollama 서버 | Llama 3, Mistral, Qwen 등 |

**멀티 LLM 구현 방향:**
- 현재 NanoClaw는 `ANTHROPIC_BASE_URL` 환경변수 지원 — 호환 엔드포인트 교체 가능
- Phase 2에서 LLM 선택 UI 추가 + 엔드포인트 추상화 레이어 개발
- 사용자가 Web UI에서 원하는 LLM 선택 → 해당 API 키 입력 or 우리 제공 선택

### 우리가 제공하는 LLM — Ollama 기반

```
우리 인프라 (Ollama 서버)
  ├── Llama 3.x     ← 범용, 무료 제공 가능
  ├── Mistral       ← 경량, 빠른 응답
  ├── Qwen          ← 다국어 강점
  └── 기타 오픈소스 모델 (수시 업데이트)

→ 사용자는 API 키 없이도 사용 가능 (플랫폼 구독료만)
→ Claude 대비 성능 차이는 있지만 비용 부담 없음
→ 프라이버시 민감 사용자에게 어필 (외부 API 미사용)
```

---

## 능동적 행동 (Proactive AI)

기존 AI와의 근본적 차이.

```
기존 AI (ChatGPT, Claude):
  사용자가 열어야만 존재함
  → 열지 않으면 아무것도 안 함 (망각형 AI)

NanoClaw:
  에이전트가 스스로 판단해서 먼저 찾아옴
  → "오늘 주식 많이 빠졌네. 확인해봤어?"
  → "3일째 운동 기록이 없어. 괜찮아?"
  → "이번 주 마감 내일인데 진행 상황 어때?"
  (살아있는 AI — 나를 알아가는 AI)
```

**능동적 행동 트리거 — 에이전트가 스스로 판단:**
- 뉴스/시장 변화 감지 → 관련 알림
- 사용자 패턴 기반 → "평소 이 시간에 확인하시잖아요"
- 마감/일정 접근 → 선제적 리마인드
- 장기 미응답 → 안부 확인
- 학습된 관심사 → 관련 정보 푸시

---

## "나를 얼마나 아는가" — 성장/진화 표현 방식

레벨 숫자보다 **에이전트가 나를 얼마나 아는지**를 자연스럽게 느끼게 하는 방식.

```
Day 1:  "안녕하세요! 저는 아직 당신을 잘 몰라요 😊"
Day 7:  "커피 마실 때 집중이 잘 되시는 것 같더라고요"
Day 30: "이런 뉴스 좋아하시잖아요, 오늘 딱 맞는 거 찾았어요"
Day 90: 말 안 해도 알아서 챙겨주는 느낌
```

**진화 메커니즘:**

NanoClaw의 핵심 차별점 — 쓸수록 나를 닮아감.

```
사용자 대화/행동
      ↓
에이전트가 패턴 감지
  - 자주 쓰는 말투
  - 선호하는 답변 길이
  - 관심 있는 주제
  - 반복되는 업무 패턴
  - 능동적 행동에 대한 반응 (좋아함 / 싫어함)
      ↓
CLAUDE.md 자동 업데이트
  (사용자 승인 or 자동)
      ↓
다음 대화 + 능동적 행동에 반영
```
사용자 대화/행동
      ↓
에이전트가 패턴 감지
  - 자주 쓰는 말투
  - 선호하는 답변 길이
  - 관심 있는 주제
  - 반복되는 업무 패턴
      ↓
CLAUDE.md 자동 업데이트
  (사용자 승인 or 자동)
      ↓
다음 대화부터 반영
```

**예시 진화 시나리오:**

| 사용자 타입 | 진화 방향                                                   |
| ----------- | ----------------------------------------------------------- |
| 개발자      | 코드 리뷰 특화, GitHub 연동, 기술 스택 메모리               |
| 작가        | 글쓰기 스타일 학습, 창작 세계관 축적, 브런치 자동 포스팅    |
| 마케터      | SNS 트렌드 분석, 콘텐츠 스케줄링, 카피 스타일 학습          |
| 투자자      | 시장 리서치 패턴, 포트폴리오 추적, 뉴스 브리핑 커스터마이징 |

---

## 온보딩 Web UI — 3계층 설정 구조

일반 사용자는 CLAUDE.md를 몰라도 됨. UI가 자연어 → CLAUDE.md 변환을 담당.

### 계층 1 — 원클릭 템플릿 (일반 사용자)

```
"어떤 AI를 원하세요?"

  🧑‍💻 개발자 도우미       ✍️  글쓰기 파트너
  📈 마케팅 어시스턴트    💼  업무 비서
  📰 뉴스 브리퍼          🎯  나만의 설정 →
```

- 선택 즉시 해당 CLAUDE.md 템플릿 자동 생성
- 추가 질문 2~3개로 세부 조정 ("어떤 채널로 받을까요?" 등)
- 기술 지식 불필요 — 클릭만으로 완료

### 계층 2 — 자연어 입력 (중간 사용자)

```
"어떤 AI가 되었으면 해?"
→ [                                        ]
  "매일 아침 뉴스 브리핑 해주고,
   내 글쓰기 스타일 기억해줬으면 해"

→ AI가 이 텍스트를 CLAUDE.md로 자동 변환
→ 변환 결과 미리보기 → 사용자 확인 → 적용
```

- 자유 텍스트 입력
- LLM이 의도 파악 → CLAUDE.md 생성
- 생성된 설정 검토/수정 가능

### 계층 3 — 직접 편집 (고급 사용자 / 개발자)

```
CLAUDE.md 직접 편집 (코드 에디터 UI)
→ 완전한 자유도
→ 버전 히스토리 + 롤백 지원
```

### 설정 가능한 항목 전체

| 항목 | 계층 1 | 계층 2 | 계층 3 |
| ---- | ------ | ------ | ------ |
| 에이전트 이름/호칭 | ✅ | ✅ | ✅ |
| 에이전트 성격/말투 | ✅ (템플릿) | ✅ (자연어) | ✅ (직접) |
| 능동적 행동 주기 | ✅ | ✅ | ✅ |
| 관심 주제 설정 | ✅ | ✅ | ✅ |
| 채널 연결 | ✅ | ✅ | ✅ |
| 스케줄 설정 | ✅ (간단) | ✅ | ✅ |
| CLAUDE.md 직접 수정 | ❌ | ❌ | ✅ |
| 추가 디렉토리 마운트 | ❌ | ❌ | ✅ |

## 아키텍처

### 전체 구조

```
[Control Plane — 신규 개발]
  - 사용자 가입 / 인증 / 결제
  - 인스턴스 생성 / 삭제 / 관리
  - API 키 암호화 저장
  - 모니터링 / 사용량 추적
          ↓
[User Instance Layer]
  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
  │  User A          │  │  User B          │  │  User C          │
  │  NanoClaw 인스턴스│  │  NanoClaw 인스턴스│  │  NanoClaw 인스턴스│
  │  독립 컨테이너    │  │  독립 컨테이너    │  │  독립 컨테이너    │
  │  독립 SQLite      │  │  독립 SQLite      │  │  독립 SQLite      │
  │  독립 CLAUDE.md   │  │  독립 CLAUDE.md   │  │  독립 CLAUDE.md   │
  │  본인 API 키      │  │  본인 API 키      │  │  우리 LLM 사용    │
  └──────────────────┘  └──────────────────┘  └──────────────────┘
```

### 비용 효율화 전략

- **Idle 컨테이너 자동 중지**: 60분 비활성 시 컨테이너 중지 (CPU/RAM 0 사용)
- **요청 시 재시작**: 메시지 수신 시 컨테이너 자동 재시작 (수 초 내)
- **상태 보존**: 중지 중에도 SQLite + CLAUDE.md 유지 → 재시작 시 이전 맥락 그대로

---

## 인프라 비용 추정

| 규모            | 서버 비용   | 구조                         |
| --------------- | ----------- | ---------------------------- |
| MVP (1~10명)    | $20~35/월   | 단일 VM (Hetzner 4vCPU/16GB) |
| 런칭 (10~50명)  | $35~50/월   | 단일 VM + idle 자동 중지     |
| 성장 (50~200명) | $100~150/월 | VM 클러스터                  |
| 스케일 (200명+) | $300+/월    | Kubernetes                   |

---

## 수익화 모델

```
플랫폼 구독료 (월)
  - Starter: 인프라 제공 + BYOK
  - Pro: 인프라 + 우리 LLM 포함 + 우선 지원
  - Team: 팀 단위 (복수 인스턴스 관리)
```

**BYOK 구조의 마진:**

- 우리 비용: 서버 인프라
- 우리 수익: 구독료
- LLM 비용: 사용자 직접 부담 → 우리 마진 리스크 없음

---

## NanoClaw 기존 코드 대비 필요한 신규 개발

| 영역 | 내용 | 예상 공수 |
| ---- | ---- | --------- |
| Control Plane API | 가입, 인스턴스 생성/삭제, 결제 | 3~4주 |
| 인스턴스 오케스트레이터 | per-user 컨테이너 생성/관리, idle 감지 | 2~3주 |
| API 키 암호화 저장 | BYOK 키 안전하게 보관, 컨테이너 주입 | 1주 |
| **온보딩 Web UI** | **3계층 설정 UI + 자연어→CLAUDE.md 변환** | **3~4주** |
| 웹 대시보드 | 인스턴스 상태, 채널 연결, 설정 변경 | 2~3주 |
| 능동적 행동 엔진 | 에이전트 스스로 판단해서 트리거 | 2~3주 |
| LLM 옵션 (우리 제공) | 프록시 레이어 + 사용량 추적 + 과금 | 1~2주 |
| NanoClaw 코드 수정 | 경로 환경변수화, 멀티 인스턴스 지원 | 1주 |

**총 예상 MVP 개발: 14~18주**

---

## Web UI 설계

> **핵심 원칙**: 온보딩 때만 쓴다. 5분 안에 나만의 AI 생성 + 채널 연결 완료.
> 이후엔 WhatsApp/Telegram에서만 살면 됨. Web UI는 설정 변경 시만 가끔.

- 플랫폼: 데스크탑 우선, 모바일은 Phase 2

---

### 온보딩 플로우 (핵심 — 이게 전부)

```
Step 1: 가입
  → 이메일 or 소셜 로그인 (Google, GitHub)
  → 계정 생성

Step 2: LLM 설정
  → (기본) 본인 Anthropic API 키 입력
         "Anthropic 콘솔에서 키 발급하는 법" 링크 제공
  → (옵션) 우리 LLM 플랜 선택 (API 키 불필요)

Step 3: 에이전트 설정 — 3계층
  → 계층 1: 원클릭 템플릿
       🧑‍💻 개발자 도우미  ✍️ 글쓰기 파트너
       📈 마케팅 어시스턴트  💼 업무 비서
       📊 투자/금융 파트너   🎯 나만의 설정 →
  → 계층 2: 자연어 입력 ("어떤 AI가 되었으면 해?")
       입력 → LLM이 CLAUDE.md 자동 생성 → 미리보기 → 확인
  → 계층 3: 직접 편집 (고급 사용자 — 언제든 접근 가능)

Step 4: 채널 연결
  → WhatsApp:  QR코드 스캔 or 전화번호 페어링 코드
  → Telegram:  봇 토큰 입력 (봇 만드는 법 가이드 제공)
  → Slack:     워크스페이스 OAuth 연결
  → Discord:   봇 초대 링크
  (하나만 연결해도 완료, 나중에 추가 가능)

Step 5: 완료
  → 🎉 "[이름]님의 AI가 준비됐어요!"
  → "지금 [채널명]에서 메시지를 보내보세요"
  → 첫 메시지 예시 힌트 제공
```

**온보딩 목표 시간: 5분 이내**

---

### 대시보드 (설정 변경 시만 접근)

```
┌─────────────────────────────────────────────┐
│  내 AI 상태                                   │
│  ● 온라인  |  마지막 활동: 2시간 전           │
│  연결 채널: WhatsApp ✅  Telegram ✅          │
├─────────────────────────────────────────────┤
│  활성 Skills/MCP                              │
│  WebSearch ✅  GitHub MCP ✅  Gmail ✅        │
│  [+ 추가하기]                                 │
├─────────────────────────────────────────────┤
│  에이전트 설정                                │
│  성격/역할 수정  |  스케줄 관리  |  알림 설정  │
├─────────────────────────────────────────────┤
│  인스턴스 관리                                │
│  재시작  |  데이터 백업  |  인스턴스 삭제      │
└─────────────────────────────────────────────┘
```

### UX 원칙

| 원칙 | 구현 방식 |
| ---- | --------- |
| **기술 용어 노출 금지** | MCP → "연결 기능", Skills → "능력", CLAUDE.md → "AI 설정" |
| **진행 상황 표시** | 온보딩 Step 표시 (1/5, 2/5...), 완료 시 축하 화면 |
| **언제든 건너뛰기** | 모든 Step에 "나중에 설정" 옵션 |
| **첫 성공 경험 빠르게** | Step 4 완료 즉시 AI가 먼저 인사 메시지 전송 |
| **실패 없는 설정** | API 키 오류 즉시 감지 + 해결 가이드 인라인 표시 |

---

## 보안 아키텍처

> SaaS 특성상 보안은 선택이 아닌 필수. 한 사용자의 데이터가 다른 사용자에게 노출되는 순간 서비스 종료.

### 기존 NanoClaw의 보안 강점 (이미 구현됨)

| 항목 | 구현 방식 |
| ---- | --------- |
| 컨테이너 격리 | 각 그룹 독립 Linux 컨테이너, non-root 실행 (uid 1000) |
| .env 차단 | `/dev/null`로 shadow mount — 에이전트가 API 키 직접 접근 불가 |
| Secrets stdin 전달 | API 키는 파일/마운트 아닌 stdin JSON으로만 주입 |
| 경로 차단 패턴 | `.ssh`, `.aws`, `.kube`, `credentials`, `id_rsa` 등 접근 불가 |
| Symlink traversal 방지 | 심볼릭 링크 해석 후 allowlist 재검증 |
| 세션 격리 | 그룹별 독립 `.claude/` 디렉토리 — 대화 이력 교차 접근 불가 |
| IPC 권한 분리 | main만 전체 관리, non-main은 본인 채팅/작업만 접근 |

### SaaS 확장 시 추가 필요한 보안

**1. API 키 암호화 저장**
```
사용자 입력 API 키
  → AES-256 암호화
  → 암호화 상태로 DB 저장
  → 컨테이너 시작 시만 복호화 → stdin 주입 → 즉시 메모리 해제
```

**2. 테넌트 간 네트워크 격리**
- 사용자 컨테이너 간 직접 통신 차단
- 외부 API 호출만 허용 (Egress 화이트리스트)
- 사용자 A의 컨테이너가 사용자 B의 데이터에 절대 접근 불가

**3. 프롬프트 인젝션 방어**
- 웹 크롤링 결과 → 에이전트 주입 전 살균 (OWASP LLM Top 1 취약점)
- MCP 도구 응답에 악성 프롬프트 삽입 가능성 차단
- 외부 입력과 시스템 프롬프트 구조적 분리

**4. 개인정보 보호 (GDPR/개인정보보호법)**
- 대화 데이터는 해당 사용자 인스턴스에만 저장
- 데이터 삭제 요청 시 완전 삭제 (인스턴스 + DB + 백업)
- 처리 목적 명시, 보존 기간 정책 수립
- EU 사용자 대상 시: 데이터 역외 이전 주의

**5. 감사 로깅**
- 모든 인스턴스 생성/삭제 기록
- API 키 접근 이벤트 기록
- 이상 트래픽 감지 (한 사용자가 비정상적으로 많은 요청)

---

## Skills / MCP 자동 제공 전략

> 일반 사용자는 Skills, MCP가 뭔지 모름. 우리가 알아서 제공해야 함.

### 개념

```
온보딩 설정 분석 (CLAUDE.md)
  → "이 사람 개발자네"
  → 개발자 Skills/MCP 자동 활성화
  → 사용자는 아무것도 몰라도 됨
```

### 직군별 자동 제공 패키지

| 직군 감지 기준 | 자동 제공 Skills/MCP |
| ------------- | ------------------- |
| **개발자** (GitHub, 코드, 배포 언급) | GitHub MCP, Linear/Jira MCP, 코드 리뷰 스킬, 음성 전사 |
| **작가/콘텐츠** (글쓰기, 브런치, SNS 언급) | WebSearch, 브런치 포스팅, 인스타 업로드, 문서 편집 |
| **마케터** (SNS, 광고, 트렌드 언급) | WebSearch, SNS 스케줄링, 트렌드 분석, 이메일 MCP |
| **투자자/금융** (주식, 코인, 포트폴리오 언급) | 금융 데이터 MCP, 뉴스 크롤링, 알림 스케줄러 |
| **일반 사용자** (특정 직군 불명확) | WebSearch, 음성 전사, 스케줄러 (기본 패키지) |

### 제공 방식

**온보딩 시점 (정적 분석):**
```
사용자 온보딩 설정 입력
  → LLM이 직군/목적 분류
  → 해당 패키지 Skills/MCP 자동 활성화
  → "이런 기능들을 활성화했어요" 요약 표시
```

**사용 중 감지 (동적 학습):**
```
대화 패턴 분석
  → "주식 얘기 자주 하네" 감지
  → "금융 데이터 연동 추가할까요?" 제안
  → 사용자 수락 → 즉시 활성화
```

### 현재 NanoClaw Skills 목록 (기반 자산)

| 카테고리 | Skills |
| -------- | ------ |
| **채널** | WhatsApp, Telegram, Slack, Discord, Gmail |
| **AI 확장** | Ollama (로컬 LLM), 병렬 에이전트, 에이전트 Swarm |
| **음성** | 음성 전사 (OpenAI Whisper), 로컬 Whisper |
| **외부 서비스** | X(Twitter), Gmail MCP |
| **개발자** | Qodo PR 리뷰, 코딩 규칙 로드 |

### MCP 확장 로드맵

```
Phase 1 (MVP): 기본 MCP만
  - WebSearch, WebFetch (이미 내장)
  - 스케줄러 (이미 내장)
  - 파일 읽기/쓰기 (이미 내장)

Phase 2: 직군별 MCP 추가
  - GitHub MCP (개발자)
  - Gmail MCP (비즈니스)
  - 금융 데이터 MCP (투자자)
  - SNS MCP (마케터/크리에이터)

Phase 3: MCP 마켓플레이스
  - 커뮤니티가 MCP 제출
  - 우리가 검수 후 제공
  - 직군별 인기 MCP 자동 추천
```

---

## 핵심 차별점 (경쟁 우위)

| 경쟁자 | 차이 |
| ------ | ---- |
| ChatGPT / Claude.ai | 공유 AI — 내 것 아님, 학습/진화 없음, 먼저 말 걸지 않음 |
| 일반 AI 챗봇 앱 | 메시징 앱 연동 없음, 컨테이너 격리 없음 |
| OpenClaw | 셀프호스팅 전용 — 기술적 허들 높음 |
| **NanoClaw SaaS** | 가입 즉시 사용 + 완전 격리 + 쓸수록 진화 + 먼저 찾아오는 AI + Skills/MCP 자동 제공 |

---

## Ideas Backlog

- 인스턴스 "성격 템플릿" 마켓플레이스 (커뮤니티 공유)
- MCP 마켓플레이스 (커뮤니티 제출 → 검수 → 제공)
- 인스턴스 백업 / 이식 (내 AI를 다른 플랫폼으로 이전)
- 팀 플랜 — 팀원들이 같은 NanoClaw 인스턴스 공유
- 사용 통계 대시보드 (얼마나 썼는지, 어떤 주제를 많이 다뤘는지)
- 오픈소스 유지 + 클라우드 유료 (Coolify 모델)
- "내 AI가 나를 얼마나 아는지" 시각화 UI (성장 지표)
- 능동적 행동 피드백 루프 ("이런 알림 좋아요/싫어요" → 즉시 반영)
- 다국어 지원 (한국어/영어/일본어 UI + 에이전트 응답)

---

## 검토 의견 — 심층 분석 (2026-03-08)

> 이 아이디어를 매우 깊게 고민한 후 정리한 실질적 평가와 제안.

### 1. 이 아이디어의 진짜 강점

**BYOK + 인프라만 제공 구조**가 기술적으로 가장 영리한 설계이다.
AI SaaS가 망하는 주는 이유 중 하나가 LLM 비용 구조 — 헤비 유저일수록 우리 손해가 커지는 구조.
는 이 모델은 그걸 원시 차단. **헤비 유저가 와도 우리 인프라 비용만 증가**하고 LLM 비용은 사용자 부담. 이건 진짜 영리한 설계다.

NanoClaw라는 **오픈소스 베이스**가 있다는 것도 큰 강점. 바닥부터 짓는 게 아니라
검증된 구조 위에 Control Plane과 UI만 어듀면 되니까 — MVP까지 가는 경로가 다른 AI 스타트업보다 훨씬 짧다.

### 2. 가장 우려되는 부분 — 온보딩 허들

**Step 2 (API 키 입력)가 최대 병목이다.**

| 대상 | 소요 시간 |
| ---- | --------- |
| 개발자 | 2분 (이미 알고 있음) |
| 일반 사용자 | 10~20분 (영문 UI, 신용카드 등록, 결제 정보...) |

�결책: **우리 LLM (Ollama) 플랜을 기본값으로 올려야 한다.**
API 키 입력은 “고급 옵션”으로 내리고, 처음엔 우리 LLM으로 바로 시작하게 하는 게 이탈률을 낙춤다.
수익 구조상으로도 Ollama 플랜이 더 안정적인 수익원이 될 수 있다.

### 3. 과소평가된 부분 — 능동적 행동 엔진

능동적 행동이 이 서비스의 **핵심 차별점**이지만, 동시에 **생사를 가르는 리스크**다.

```
너무 자주 말 걸면  → 스팬, 알림 꺼고 싶어집 (이탈)
너무 안 말 걸면  → 기존 AI랑 차이 없음 (이탈)
타이밍이 안 맞으면 → 짜증남, 신뢰 파괴 (이탈)
```

권장: 시작은 **매우 보수적**으로. 사용자가 먼저 패턴을 쌓게 하고, 신뢰가 생긴 후에 능동성을 높이는 방식.
“알림 좋았어요/싫어요” 피드백 루프가 없으면 능동적 행동은 참사가 뤎어.

### 4. 경쟁 포지셔닝

ChatGPT/Claude.ai와 직접 경쟁하면 진다. **다른 카테고리**로 포지셔닝해야 한다.

```
ChatGPT / Claude.ai:  “질문하면 답해주는 AI”  (도구)
NanoClaw SaaS:         “나를 위해 일하는 AI 직원”  (존재)
```

마케팅 메시지: **“더 나은 ChatGPT”가 아니라 “AI 직원 고용”**으로 잡아야 한다.

### 5. 놓친 설계 — 쳋 주 경험

**온보딩 이후 첫 주 시퀀스가 리텐션을 결정한다.**

```
Day 0 (온보딩 완료):
  AI: "안녕하세요! 저 [AI 이름]이에요. 지금부터 열심히 해볼게요 😊"

Day 1:
  AI: "어제 첫 대화 했는데, 저한테 가장 많이 부탁하고 싶은 게 뮤에요?"

Day 3:
  AI: "관심사가 [XX]인 것 같더라고요.
       매일 아침 [관련 정보] 보내드릴까요?"

Day 7:
  AI: "일주일 됨는데! 제가 이런 걸 파악했어요: [요약]
       맞게 이해한 건가요?"
```

이 **쳋 주 시퀀스**가 리텐션을 결정한다. 여기서 “이 AI 나를 알아가고 있구나”를 느끼면 이탈률이 몿 떨어진다.
**TRD 단계에서 쳍 주 시퀀스 설계를 반드시 포함할 것.**

### 6. 수익 모델 보완

**Ollama 기반 우리 LLM 플랜이 수익의 핵심이 될 수 있다.**

```
BYOK 플랜:
  우리 수익 = 구독료 - 서버 인프라 비용
  LLM 비용은 사용자 부담 → 마진 리스크 없음

우리 LLM (Ollama) 플랜:
  우리 수익 = 구독료 - (서버 + GPU/CPU 비용)
  오픈소스 모델이라 구독료이 충분하면 권늹 투자 후 높은 마진 가능
```

**결론**: Ollama 플랜을 잘 설계하면 BYOK보다 수익성이 높은 플랜이 된다.
이 플랜으로 진입 장벽을 낙춰(API 키 불필요) + 수익도 높이는 Two-for-one 전략 가능.

### 종합 평가

> **아이디어 자체는 💪 탄탄하다.**  
> 기술 베이스도 있고, 비용 구조도 영리하고, 차별점도 명확하다.  
> **성패는 두 가지가 결정할 거다**:
> 1. **온보딩 허들 제거** — 특히 API 키 문제. Ollama 플랜을 기본값으로.
> 2. **능동적 행동 엔진의 품질** — 너무 자주도 너무 조용해도 안 됨. 쳋 주 시퀀스 설계 리텐션 핑심.

---

## 런칭 전략 — 서비스 → 랜딩 페이지 → 마케팅

> 업데이트: 2026-03-15
> 전략: 서비스 배포 후 랜딩 페이지로 유입 창구 확보 → 6개월 반응 측정

### 핵심 판단

```
이 게임은 기술 싸움이 아닙니다.
패키징 + 온보딩 + 마케팅이 성패를 결정합니다.

"더 나은 ChatGPT"로 포지셔닝하면 집니다.
"AI 직원 고용"으로 포지셔닝해야 합니다.

ChatGPT / Claude.ai:  질문하면 답해주는 도구
NanoClaw SaaS:        나를 위해 일하는 AI 직원
```

---

### 포지셔닝 전략

**피해야 할 메시지:**
```
❌ "더 스마트한 AI 챗봇"
❌ "나만의 ChatGPT"
❌ "AI 어시스턴트"
→ 이미 레드오션, 차별점 없음
```

**가야 할 메시지:**
```
✅ "AI 직원을 고용하세요"
✅ "당신이 자는 동안 일하는 AI"
✅ "말 안 해도 알아서 챙겨주는 AI"
→ 도구가 아닌 존재로 포지셔닝
```

---

### 타겟 페르소나 (MVP 단계 — 하나만)

```
첫 타겟: 1인 사업자 / 프리랜서

이유:
  - AI 도입 욕구 높음
  - 비서/직원 고용 부담 느낌
  - "AI 직원" 메시지에 공감 높음
  - ₩19,900/월 결제 허들 낮음
  - 입소문 빠름

구체적 프로필:
  - 유튜버, 작가, 컨설턴트, 디자이너
  - 혼자 일하면서 반복 업무에 지쳐있음
  - 매일 확인해야 할 것들이 많음
  - "누가 대신 챙겨줬으면" 하는 니즈
```

---

### 랜딩 페이지 메시지 구조

#### Hero 섹션 (첫 화면 — 3초 안에 이해)

```
헤드라인:
  "AI 직원을 고용하세요"

서브헤드:
  "당신이 자는 동안 뉴스를 읽고,
   아침에 일어나면 브리핑이 와있는 AI.
   쓸수록 당신을 닮아갑니다."

CTA 버튼:
  "5분 만에 내 AI 만들기" → 무료 체험

소셜 프루프:
  "지금 XXX명이 사용 중"
```

#### 문제 섹션 (공감)

```
"이런 경험 있으신가요?"

  😮‍💨 매일 아침 뉴스/메일을 확인하는 데 1시간
  📋 반복되는 업무를 또 직접 하고 있음
  🔕 중요한 알림을 놓쳐서 뒤늦게 후회
  💭 "누가 이것 좀 대신 해줬으면..."

→ 이제 AI 직원에게 맡기세요.
```

#### 솔루션 섹션 (차별점)

```
기존 AI와 다른 점:

"ChatGPT는 열어야만 존재합니다.
 당신의 AI는 열지 않아도 일합니다."

  ✅ 매일 아침 뉴스 브리핑이 먼저 와있음
  ✅ 마감이 다가오면 AI가 먼저 알려줌
  ✅ 쓸수록 내 말투, 내 취향을 닮아감
  ✅ WhatsApp / Telegram에서 바로 사용
```

#### 작동 방식 섹션 (신뢰)

```
"5분이면 됩니다"

  Step 1: 가입
  Step 2: 어떤 AI가 되길 원하는지 설명
  Step 3: 카카오톡 / WhatsApp 연결
  Step 4: AI가 먼저 인사를 건넵니다

→ 코딩 지식 불필요
→ API 키 없어도 시작 가능
```

#### 요금제 섹션

```
Starter  ₩9,900/월
  - AI 인스턴스 1개
  - 기본 채널 1개 연결
  - 하루 50회 대화

Pro  ₩19,900/월  ← 추천
  - AI 인스턴스 1개
  - 채널 3개 연결 (WhatsApp + Telegram + Slack)
  - 무제한 대화
  - 능동적 알림 기능
  - 우리 LLM 제공 (API 키 불필요)

→ 14일 무료 체험 (카드 불필요)
```

#### CTA 섹션 (하단)

```
"지금 내 AI를 만들어보세요"

첫 14일은 무료입니다.
마음에 들지 않으면 클릭 한 번에 해지.

[5분 만에 시작하기]
```

---

### 마케팅 채널 전략 (6개월)

#### 1~2개월 — 콘텐츠 기반 유입 (비용 0)

```
블로그/브런치:
  "AI 직원을 고용했더니 생긴 일"
  "혼자 일하는 사람이 AI를 쓰는 법"
  "ChatGPT와 다른 AI를 써봤더니"

커뮤니티:
  - 디스콥트, 인디해커스
  - 1인 사업자 커뮤니티
  - 프리랜서 카카오톡 오픈채팅

ProductHunt 런칭:
  - 글로벌 유입 창구
  - 영어 랜딩 페이지 병행
```

#### 3~4개월 — 바이럴 포인트 설계

```
추천인 프로그램:
  "친구 초대하면 한 달 무료"
  → 사용자가 직접 마케터가 됨

스크린샷 바이럴:
  "아침에 일어났더니 AI가 보낸 브리핑"
  → SNS에 자연스럽게 공유되는 경험 설계
  → 온보딩 완료 시 공유 유도 화면 삽입
```

#### 5~6개월 — 데이터 기반 최적화

```
측정 지표:
  - 랜딩 → 가입 전환율 (목표: 5% 이상)
  - 가입 → 유료 전환율 (목표: 20% 이상)
  - 월 이탈률 (목표: 10% 이하)
  - NPS (목표: 40 이상)

이탈 포인트 분석:
  어느 단계에서 떠나는가
  → 거기서 개선 집중

6개월 후 판단:
  전환율 좋음 → 마케팅 예산 투입, 확장
  전환율 나쁨 → 메시지/타겟 피벗
  반응 없음   → 동시통역으로 전환
```

---

### 온보딩 성공 기준

```
핵심 지표: "첫 주 내에 AI가 먼저 말 걸어오는 경험"

Day 0: 온보딩 완료 → AI가 먼저 인사
Day 1: AI가 "어제 첫 대화를 했는데..." 로 말 걸어옴
Day 3: AI가 관심사 파악 → "이런 거 보내드릴까요?" 제안
Day 7: "일주일 됐어요! 제가 파악한 것들이에요" 요약

→ 여기까지 경험한 사용자는 이탈률 급감
→ 온보딩 완료율과 Day 7 도달률이 핵심 KPI
```

---

### 런칭 체크리스트

```
서비스:
  □ NanoClaw 인스턴스 자동 생성 (Control Plane)
  □ Ollama LLM 플랜 기본값 설정
  □ WhatsApp / Telegram 채널 연결
  □ 첫 주 시퀀스 메시지 설계

랜딩 페이지:
  □ Hero 섹션 (헤드라인 + CTA)
  □ 문제/솔루션 섹션
  □ 요금제 섹션
  □ 14일 무료 체험 버튼
  □ 모바일 최적화

마케팅:
  □ 브런치/블로그 첫 글 발행
  □ ProductHunt 등록 준비
  □ 커뮤니티 계정 생성
  □ 추천인 프로그램 설계

측정:
  □ Google Analytics 설치
  □ 전환 이벤트 설정 (가입, 결제)
  □ 이탈 포인트 트래킹
```

---

## Control Plane 설계

> 업데이트: 2026-03-15
> NanoClaw SaaS의 핵심 기술 — 모든 사용자 컨테이너의 두뇌

### 한 줄 정의

```
Control Plane = NanoClaw SaaS의 두뇌

사용자가 1,000명이어도
컨테이너 1,000개가 동시에 돌지 않습니다.
활성 사용자만 켜고, 나머지는 중지 상태 유지.
실제 사용한 만큼만 비용 발생.

Control Plane이 모든 생명주기를 관리합니다.
```

---

### ECS에서 컨테이너가 동작하는 방식

```
사용자 1,000명 = 컨테이너 1,000개 동시 실행 ❌  (비용 폭발)
사용자 1,000명 = 활성 컨테이너 20~50개 + 나머지 중지 ✅

핵심 규칙:
  활성 상태:  메시지 수신 시 컨테이너 자동 시작
  유휴 상태:  60분 비활성 시 컨테이너 자동 중지
  상태 보존:  중지 중에도 SQLite + CLAUDE.md 유지
              → 재시작 시 이전 맥락 그대로 복원
```

### 실제 동작 흐름

```
사용자가 WhatsApp으로 메시지 전송
        ↓
Control Plane (항상 실행 중 — 작은 서버 1대)
  "user_123의 메시지 수신"
        ↓
user_123 컨테이너 상태 확인
  ├── 실행 중 → 바로 전달
  └── 중지됨 → 컨테이너 재시작 (10~30초)
        ↓
에이전트 응답
        ↓
60분 동안 추가 메시지 없으면
  → 컨테이너 자동 중지 (CPU/RAM 0)
```

### 능동적 행동 (에이전트가 먼저 말 걸기)

```
컨테이너가 중지되어 있어도 능동적 행동이 가능한 이유:

능동적 행동 스케줄러는 Control Plane에서 실행 (항상 켜져 있음)

"user_123에게 아침 브리핑 전송 시간"
        ↓
Control Plane이 컨테이너 깨움
        ↓
에이전트 실행 → 메시지 전송
        ↓
완료 후 다시 중지

→ 사용자는 AI가 항상 깨어있는 것처럼 느낌
→ 실제 비용은 실행 시간만 발생
```

---

### 전체 아키텍처

```
┌─────────────────────────────────────────────┐
│              Control Plane                   │
│  (NestJS — ECS Fargate 1대, 항상 실행)       │
│                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  API     │  │Scheduler │  │Container │  │
│  │  Server  │  │  Engine  │  │ Manager  │  │
│  └──────────┘  └──────────┘  └──────────┘  │
│                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  Auth    │  │ Webhook  │  │ Monitor  │  │
│  │  Service │  │ Handler  │  │ Service  │  │
│  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────┘
         ↓               ↓
┌─────────────┐  ┌─────────────┐
│  Supabase   │  │    Redis    │
│  (상태 저장) │  │  (큐/캐시)  │
└─────────────┘  └─────────────┘
         ↓
┌─────────────────────────────────┐
│         ECS (사용자 컨테이너)     │
│  [user_001] [user_002] [...]    │
│  running    stopped   stopped   │
└─────────────────────────────────┘
```

---

### 6개 컴포넌트

#### 1. API Server — 외부 진입점

```
담당:
  - 사용자 가입/로그인
  - 인스턴스 생성/삭제
  - 설정 변경 (CLAUDE.md 업데이트)
  - 과금 처리

핵심 엔드포인트:
  POST   /instances            인스턴스 생성
  DELETE /instances/:id        인스턴스 삭제
  GET    /instances/:id/status 상태 조회
  PUT    /instances/:id/config 설정 변경
  POST   /auth/signup
  POST   /billing/subscribe
```

#### 2. Container Manager — 생명주기 핵심

```
담당:
  - ECS 태스크 시작/중지
  - 상태 추적 (running / stopped / starting)
  - 콜드스타트 처리 (10~30초 대기)
  - Idle 감지 → 자동 중지 (60분 기준)

핵심 로직:
  메시지 수신 시:
    1. 컨테이너 상태 확인
    2. 중지 상태면 ECS 태스크 시작
    3. 준비될 때까지 대기
    4. 메시지 전달

  매 5분 체크:
    1. 실행 중인 컨테이너 목록 조회
    2. 마지막 활동 시간 확인
    3. 60분 초과 시 자동 중지
```

#### 3. Webhook Handler — 채널 메시지 수신

```
담당:
  - WhatsApp / Telegram / Slack 웹훅 수신
  - 메시지를 해당 사용자 컨테이너로 라우팅
  - 마지막 활동 시간 갱신

처리 순서:
  웹훅 수신 → 사용자 식별 → 컨테이너 확보 → 메시지 전달
```

#### 4. Scheduler Engine — 능동적 행동 핵심

```
담당:
  - 사용자별 스케줄 관리
  - 정해진 시간에 컨테이너 깨워서 실행
  - 이벤트 기반 트리거

스케줄 예시:
  매일 오전 8시  → 아침 뉴스 브리핑
  매주 금요일    → 주간 업무 정리 알림
  마감 D-1      → 선제적 리마인드
  장기 미응답    → 안부 확인

동작:
  매 분 크론 실행
  → 실행할 스케줄 조회
  → 컨테이너 깨우기
  → 명령 전달 → 완료 후 중지
```

#### 5. Auth Service — 인증/과금

```
담당:
  - JWT 토큰 발급/검증
  - 플랜 확인 (free / starter / pro)
  - API 키 AES-256 암호화 저장
  - 컨테이너 시작 시 복호화 → stdin 주입 → 메모리 해제
  - 사용량 추적 및 제한 체크

플랜별 제한:
  Starter  ₩9,900  채널 1개 / 일 50회
  Pro      ₩19,900 채널 3개 / 무제한 / 능동적 알림
```

#### 6. Monitor Service — 이상 감지

```
담당:
  - 컨테이너 크래시 감지 → 자동 재시작
  - 비용 이상 감지 (비정상적 리소스 사용)
  - 전체 시스템 헬스체크 (5분 주기)
  - Slack 알림 발송
```

---

### Supabase 스키마 (Control Plane용)

```sql
-- 인스턴스 상태
CREATE TABLE instances (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id         UUID REFERENCES users(id),
  status          TEXT DEFAULT 'stopped',  -- running|stopped|starting
  ecs_task_arn    TEXT,                    -- 실행 중인 ECS 태스크 ARN
  last_activity   TIMESTAMPTZ,
  created_at      TIMESTAMPTZ DEFAULT NOW()
);

-- 스케줄 (능동적 행동)
CREATE TABLE schedules (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id     UUID REFERENCES users(id),
  action      TEXT NOT NULL,       -- morning_briefing|reminder|...
  cron        TEXT NOT NULL,       -- '0 8 * * *'
  params      JSONB DEFAULT '{}',
  is_active   BOOLEAN DEFAULT TRUE,
  last_run    TIMESTAMPTZ,
  created_at  TIMESTAMPTZ DEFAULT NOW()
);

-- 채널 연결
CREATE TABLE channels (
  id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id      UUID REFERENCES users(id),
  type         TEXT NOT NULL,     -- whatsapp|telegram|slack
  identifier   TEXT NOT NULL,     -- 전화번호|chat_id|webhook_url
  is_active    BOOLEAN DEFAULT TRUE,
  created_at   TIMESTAMPTZ DEFAULT NOW()
);

-- 사용량 추적
CREATE TABLE usage_logs (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id     UUID REFERENCES users(id),
  action      TEXT NOT NULL,
  created_at  TIMESTAMPTZ DEFAULT NOW()
);
```

---

### 비용 구조

```
Control Plane (항상 실행):
  ECS Fargate t3.small 1대
  → 월 약 ₩45,000 고정

사용자 컨테이너:
  활성 시간만 과금 (60분 Idle 시 자동 중지)
  사용자 1,000명 × 평균 30분/일 × 30일
  = 15,000 컨테이너-시간/월
  → 월 약 ₩225,000~300,000

총합 (사용자 1,000명 기준):
  월 비용:  ₩270,000~345,000
  1인당:    ₩270~345

수익 계산:
  ₩19,900 × 1,000명 = ₩19,900,000 매출
  비용      ₩345,000
  → 마진율 약 98%
```

---

### 구현 순서

```
1단계: Control Plane 뼈대
  □ NestJS 프로젝트 셋업
  □ Supabase 연결
  □ 사용자 가입 → 인스턴스 생성 API
  □ ECS 태스크 시작/중지 구현

2단계: Webhook Handler
  □ Telegram 웹훅 수신
  □ 컨테이너 ensureRunning → 메시지 전달
  □ 마지막 활동 시간 갱신

3단계: Scheduler Engine
  □ 크론 기반 스케줄 실행
  □ 능동적 행동 트리거
  □ 컨테이너 깨우기 → 명령 전달

4단계: 과금/모니터링
  □ 플랜 제한 체크
  □ API 키 암호화 저장
  □ 이상 감지 + 자동 재시작
```

---

## 결제 설계 (Lemon Squeezy 기반)

> 업데이트: 2026-03-15
> PG사: Lemon Squeezy (Merchant of Record — 한국 사업자 직접 가입 가능)
> 방식: 14일 무료 체험 → 자동 결제 전환 + 월/연 구독

---

### Lemon Squeezy를 선택한 이유

```
Stripe:
  장점: 기능 풍부, SaaS에 최적화, 수수료 저렴 (2.9%)
  단점: 한국 법인으로 가입 불가 ❌
        해외 법인 설립 필요 (수백만원 + 수개월)

Lemon Squeezy:
  장점: 한국 사업자로 바로 가입 ✅
        Merchant of Record → 글로벌 세금(VAT/GST) 자동 처리
        135개국 결제 지원
        구독 관리 내장 (업그레이드/다운그레이드/일할 계산)
        이메일 마케팅 통합
  단점: 수수료 약 5% + $0.50 (Stripe보다 비쌈)

→ MVP 단계: Lemon Squeezy (빠른 시작, 한국 법인)
→ 사용자 100명+ 이후: Stripe 전환 고려 (수수료 절감)
   단, 구독 마이그레이션 시 서비스 중단 발생 — 미리 계획 필요
```

---

### 요금제 구조

| 플랜 | 월 구독 | 연 구독 | 혜택 |
|------|---------|---------|------|
| **Starter** | ₩9,900/월 | ₩95,000/년 (20% 할인) | 채널 1개 / 일 50회 대화 |
| **Pro** | ₩19,900/월 | ₩190,000/년 (20% 할인) | 채널 3개 / 무제한 / 능동적 알림 / 우리 LLM 포함 |

```
무료 플랜 없음.
14일 무료 체험 → 카드 등록 후 시작 → 만료 시 자동 결제.
체험 기간 내 해지 시 카드 미청구.
```

---

### 결제 플로우

```
[온보딩 Step 2 — 요금제 선택]

  Starter ₩9,900    Pro ₩19,900  ← 추천 뱃지
  월간 / 연간 토글

  [14일 무료로 시작하기]
        ↓
  Lemon Squeezy Checkout (카드 정보 입력)
  "14일 후 자동 결제됩니다. 언제든 해지 가능."
        ↓
  결제 정보 등록 완료
  → Lemon Squeezy Subscription 생성 (status: on_trial)
        ↓
  인스턴스 생성 → 에이전트 시작
        ↓
  [14일 후]
  Lemon Squeezy가 자동으로 첫 결제 처리
  → status: active
        ↓
  매월/매년 자동 갱신
```

---

### Lemon Squeezy 핵심 객체

```
Store        → 우리 스토어 (1개)
Product      → NanoClaw SaaS
Variant      → Starter 월간 / Starter 연간 / Pro 월간 / Pro 연간
Customer     → 사용자 1명당 1개 생성
Subscription → 플랜 + 상태 (on_trial / active / cancelled / past_due)
Order        → 매 결제 청구서 자동 발행
```

---

### Webhook 이벤트 처리

```
Lemon Squeezy가 결제 상태 변화 시 Control Plane으로 웹훅 전송
Control Plane이 수신 → 인스턴스 상태 업데이트

처리해야 할 핵심 이벤트:

subscription_created
  → 인스턴스 활성화 (on_trial 상태)

subscription_updated (trial_ends_at 3일 전)
  → 사용자에게 알림 (Telegram/Slack)
  → "3일 후 ₩19,900 결제 예정입니다"

order_created (결제 성공)
  → 구독 유지
  → 영수증 이메일 발송 (Lemon Squeezy 자동 처리)

subscription_payment_failed
  → 결제 실패 → 사용자 알림
  → 3일 유예 후 인스턴스 일시 정지
  → 카드 업데이트 유도

subscription_cancelled
  → 해지 처리 → 인스턴스 중지
  → 데이터는 30일 보관 후 삭제
```

---

### 해지/환불 정책

```
해지:
  언제든 가능 (대시보드에서 클릭 한 번)
  해지 시 남은 기간까지 서비스 유지
  다음 결제일부터 미청구

환불:
  체험 기간 내 해지 → 100% 환불 (미청구)
  결제 후 7일 이내 → 전액 환불
  7일 이후 → 환불 없음 (정책 명시)

데이터:
  해지 후 30일간 데이터 보관
  30일 후 완전 삭제
  재가입 시 복원 불가 (명시)
```

---

### Supabase 스키마 추가 (결제용)

```sql
-- 구독 정보
CREATE TABLE subscriptions (
  id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id             UUID REFERENCES users(id),
  ls_customer_id      TEXT UNIQUE NOT NULL,   -- Lemon Squeezy Customer ID
  ls_subscription_id  TEXT UNIQUE NOT NULL,   -- Lemon Squeezy Subscription ID
  ls_variant_id       TEXT NOT NULL,          -- 어떤 Variant (플랜/주기)
  plan                TEXT NOT NULL,          -- starter | pro
  billing_cycle       TEXT NOT NULL,          -- monthly | annual
  status              TEXT NOT NULL,          -- on_trial | active | cancelled | past_due
  trial_ends_at       TIMESTAMPTZ,
  current_period_end  TIMESTAMPTZ,
  cancelled_at        TIMESTAMPTZ,
  created_at          TIMESTAMPTZ DEFAULT NOW(),
  updated_at          TIMESTAMPTZ DEFAULT NOW()
);

-- 결제 이력
CREATE TABLE payments (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id         UUID REFERENCES users(id),
  ls_order_id     TEXT UNIQUE NOT NULL,   -- Lemon Squeezy Order ID
  amount          INT NOT NULL,           -- 원화 (₩)
  status          TEXT NOT NULL,          -- paid | failed | refunded
  paid_at         TIMESTAMPTZ,
  created_at      TIMESTAMPTZ DEFAULT NOW()
);
```

---

### 플랜별 기능 제한 체크

```
인스턴스 실행 전 Control Plane이 확인:

1. 구독 상태 확인
   on_trial / active → 통과
   cancelled / past_due → 인스턴스 중지 + 결제 유도

2. 플랜별 제한 확인
   Starter: 일 50회 초과 시 → "Pro로 업그레이드하세요"
   Pro: 무제한 통과

3. 트라이얼 만료 3일 전
   → Telegram/Slack으로 자동 알림 발송
```

---

### 업그레이드/다운그레이드

```
Starter → Pro 업그레이드:
  Lemon Squeezy가 일할 계산 자동 처리
  즉시 Pro 기능 활성화

Pro → Starter 다운그레이드:
  현재 결제 기간 만료 후 적용
  기간 중 Pro 기능 유지

월간 → 연간 전환:
  즉시 적용 + 일할 계산 크레딧 처리
  Lemon Squeezy가 자동 처리
```

---

### Phase별 결제 전환 계획

```
Phase 1 (MVP ~ 사용자 100명):
  Lemon Squeezy 유지
  한국 사업자로 글로벌 결제 즉시 처리
  세금 자동 처리 (VAT/GST)
  수수료: 약 5% + $0.50/건

Phase 2 (사용자 100명+):
  Stripe 전환 검토
  수수료: 2.9% + $0.30/건 → 마진 개선
  단, 마이그레이션 시 고객 카드 재등록 필요
  → 고객 커뮤니케이션 필수 (이탈 방지)
  → 해외 법인 설립 또는 Stripe Atlas 활용
```

---

### 준비 체크리스트

```
□ Lemon Squeezy 계정 생성 (lemonsqueezy.com)
   → 한국 사업자 정보로 가입
   → 원화 또는 달러 정산 계좌 등록

□ Store + Product 생성
   → Product: "NanoClaw SaaS"
   → Variant 4개 생성:
      - Starter 월간: ₩9,900 (monthly)
      - Starter 연간: ₩95,000 (yearly)
      - Pro 월간:     ₩19,900 (monthly)
      - Pro 연간:     ₩190,000 (yearly)

□ 14일 Free Trial 설정
   → 각 Variant에 trial_interval_count: 14 설정

□ Webhook 엔드포인트 등록
   → Control Plane /billing/webhook
   → 서명 검증 키 저장

□ 이용약관 + 환불정책 페이지 작성
   → Lemon Squeezy 심사 필수 요건

□ 결제 실패 Dunning 설정
   → 자동 재시도 주기 설정 (Lemon Squeezy 내장)

□ 영수증 이메일 템플릿 설정
   → Lemon Squeezy 자동 발송 (별도 개발 불필요)
```

---

## 서비스 시작을 위한 인프라 준비

### 필수 (없으면 시작 못함)

```
✅ Supabase        → DB + 인증
✅ AWS ECS + ECR   → 컨테이너 실행/저장
✅ AWS EFS or EBS  → SQLite + CLAUDE.md 영속 저장
                     (컨테이너 중지해도 데이터 유지)
```

### 채널 연결용 (에이전트가 말 걸려면)

```
✅ Telegram Bot    → @BotFather에서 생성, 가장 쉬움
✅ Slack 앱        → api.slack.com → Create App
   (WhatsApp은 Meta 비즈니스 계정 심사 2~4주 → 나중에)
```

### 결제 (유료 전환하려면)

```
✅ Lemon Squeezy   → 한국 사업자 직접 가입 가능
                     글로벌 세금(VAT/GST) 자동 처리
                     구독 관리 내장
```

---

### 준비 우선순위

```
1순위 (오늘 — 개발 시작 가능):
  □ Supabase 가입
  □ AWS 계정 + ECS / ECR / EFS 권한 설정
  □ Telegram Bot 생성 (@BotFather)
  □ Slack 앱 등록 (api.slack.com → Create App)

2순위 (MVP 직전):
  □ Lemon Squeezy 가입 + Store/Product 설정
  □ 도메인 구입 (Route53 or 가비아)
  □ 이용약관 + 환불정책 페이지 작성 (Lemon Squeezy 심사 필수)

3순위 (나중에):
  □ WhatsApp Business API (Meta 심사 2~4주)
  □ Discord 봇 등록
```
